# Boilerplate for a typescript npm module

## Usage

```
git clone https://github.com/lioneltay/typescript-package-cli.git <name>
```

Eventually this will become a cli tool.

## TODO
- [ ] cli tool (at the moment just have to clone the repo)
